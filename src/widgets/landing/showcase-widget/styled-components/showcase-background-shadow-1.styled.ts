import { styled } from "goober";

export const ShowcaseBackgroundShadow1Styled = styled('div')`
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  display: flex;
  background: rgba(0, 0, 0, 0.5);
`;