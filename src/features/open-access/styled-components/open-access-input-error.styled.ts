import { styled } from "goober";

export const OpenAccessInputErrorStyled = styled('div')`
  display: flex;
  flex-direction: row;
  align-items: center;
  column-gap: 2px;

  font-size: 12px;
  font-weight: 400;
  line-height: 13px;
  letter-spacing: -0.5px;
  text-align: left;
  text-underline-position: from-font;
  text-decoration-skip-ink: none;
  color: rgba(228, 13, 13, 1);
`;